import { createSlice } from "@reduxjs/toolkit";

const initialState = {};

export const createOutlayRowSlice = createSlice({
  name: "createOutlayRowSlice",
  initialState,
  reducers: {},
});
export const createOutlayRowReducer = createOutlayRowSlice.reducer;
